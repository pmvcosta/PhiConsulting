import { WidgetOptions } from './widget-options';
export declare type Widget = {
    refresh: () => void;
    unmount: () => void;
};
export declare const createWidget: (formId: string, options: WidgetOptions) => Widget;
