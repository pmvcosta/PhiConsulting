import { BaseOptions, EmbedType, UrlOptions } from '../base';
export declare const buildIframeSrc: (params: BuildIframeSrcOptions) => string;
declare type BuildIframeSrcOptions = {
    formId: string;
    embedId: string;
    type: EmbedType;
    options: BaseOptions & UrlOptions;
};
export {};
